# Build stage
FROM node:20-alpine AS build
WORKDIR /app
COPY ui/package*.json ui/tsconfig*.json ui/vite.config.* ui/ ./
RUN npm install
COPY ui/. .
RUN npm run build

# Serve stage
FROM nginx:1.27-alpine
COPY --from=build /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
